# 待解决问题 TODO 列表

## 高优先级

1. **提升四格日报成品质量与稳定性**
   - 目标：继续优化 `事实 / 影响 / 判断 / 动作` 四格内容质量，减少模板化和空话；同时强化 `score/insight` 阶段的重试与回退，避免供应商瞬断把选题池拉歪。
   - 状态：待开始

2. **为 LLM 调用增加全局并发限流器**
   - 目标：把 `callLLM / callLLMJson` 升级为全局受控并发模式，统一限制总并发和单 provider 并发；默认按 `总并发 8 / 单 provider 4 / pro 2 / flash 4` 运行，并加上 `429/503` 自动降速、抖动退避和队列调度，优先优化 `gate-keep / score / insight` 的串行批处理耗时。
   - 状态：已完成

3. **实现全量 24 小时抓取**
   - 目标：修改 Folo 抓取逻辑，不再限 10 页，而是持续拉取直到超出 24h 范围。
   - 状态：待开始

4. **优化新闻分类与精选逻辑**
   - 目标：确保 AI、Tech 等核心分类每个都能筛选出 3-5 条高质量内容。
   - 状态：待开始

5. **日报结构优化：增加“更多参考”列表**
   - 目标：在深度解读后，增加过滤掉低质内容的普通新闻列表，展示 24h 内的其他动态。
   - 状态：待开始

6. **强化“投资理财”分类内容**
   - 目标：侧重分析 24h 市场影响并给出操作建议，优化专用 Prompt。
   - 状态：待开始


## 中优先级

7. **改进分类逻辑，确保每个类型的新闻都有 3-4 条**
   - 当前状态：待开始
   - 目标：优化分类逻辑，确保各分类新闻数量均衡

8. **优化日志输出，更清晰地显示抓取过程和结果**
   - 当前状态：待开始
   - 目标：改进日志格式，提高可读性

9. **优化 gate-keep 过滤逻辑，提高新闻质量**
   - 当前状态：待开始
   - 目标：改进过滤规则，减少噪声

10. **添加错误处理和重试机制，提高抓取稳定性**
   - 当前状态：待开始
   - 目标：增加错误处理和重试逻辑，提高抓取稳定性

## 已完成任务

- [x] 检查 Folo 列表配置，确保获取所有订阅源
- [x] 优化 Folo API 调用，获取更多新闻条目

## 备注

- 目前 Folo 列表只获取到 2-3 个订阅源，可能需要进一步调查 API 响应
- 每个 Folo 源只返回 4 条新闻，可能是 API 限制
- 抓取流程已基本正常，需要进一步测试和优化
