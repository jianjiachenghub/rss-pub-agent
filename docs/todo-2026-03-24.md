# TODO 2026-03-24: 个人日报输入层治理

## 已完成：抓取入口从“大流量聚合”切到“主池 + 观察池”
- [x] 为 feed 增加 `tier`、`dailyCap`、`keepInMainPool` 三个治理字段。
- [x] `fetch.ts` 不再使用“`folo-list >= 50` 就跳过其他源”的逻辑。
- [x] 高频大杂烩源和热榜源默认退出主日报主池，仅在主池不足时回填。
- [x] `folo` 直连源补上 24 小时过滤，`folo-list` 支持按上限提前停止。

## 1. 继续优化抓取层：分类保底而不是单纯抓更多
- **目标**: 日报候选集优先保证分类覆盖和信噪比，而不是追求抓取总量。
- **操作**:
  - [ ] 给 `investment` 补充直连源，不再只依赖聚合入口。
  - [ ] 为 `business / politics / investment` 增加更稳定的一手或高质量二手源。
  - [ ] 根据实际运行结果微调各分类的 raw cap。

## 2. 修正评分偏置：从“AI 日报”转向“个人多分类日报”
- **目标**: 降低 AI 主题对评分与入选的天然优势。
- **操作**:
  - [ ] 调整 `configs/prompt.json` 的兴趣结构，补充泛科技、商业、投资关注项。
  - [ ] 让 `scoreNode` 更明确地考虑分类平衡，而不是只在候选阶段做配额。
  - [ ] 重新校准 `topN` 与 `TARGET_TOTAL_ITEMS` 的关系，避免配置失真。

## 3. 修正“更多 24h 资讯”的分类错误
- **目标**: 让末尾参考列表不再沿用抓取时的粗糙标签。
- **操作**:
  - [ ] 对 `secondaryItems` 也执行快速分类。
  - [ ] 清理原始聚合标签导致的 “非 AI 内容堆进 AI 分类” 问题。
  - [ ] 为参考列表增加更严格的去重和质量阈值。

## 4. 重新定义日报成品
- **目标**: 让最终产物从“AI 日报”升级为真正的“个人日报”。
- **操作**:
  - [ ] 调整标题和导语，不再默认全局 framing 为 `AI 日报`。
  - [ ] 将“今日概览”改成真正的多分类总览，而不是 AI 叙事为主。
  - [ ] 明确哪些分类属于必看，哪些属于补充观察。

## 5. 投资理财专题单独增强
- **目标**: 等输入源和分类稳定后，再强化投资理财洞察深度。
- **操作**:
  - [ ] 补齐投资源后，再为 `investment` 分类单独优化 Prompt。
  - [ ] 在洞察中加入市场影响、风险提示、操作建议三段式结构。
  - [ ] 评估是否需要把投资类日报拆成独立分发通道。

---
*上次更新于 2026-03-26*
