# TODO 2026-03-24: 抓取与分发逻辑优化

## 1. 抓取逻辑升级：全量 24 小时抓取
- **目标**: 不再局限于 10 页，而是实现真正的“全量”抓取，直到分页条目时间超过过去 24 小时为止。
- **文件**: `scripts/lib/folo.ts`
- **操作**: 
  - [ ] 修改 `MAX_PAGES` 限制或根据条目时间戳自动决定是否继续拉取下一页。
  - [ ] 确保 `publishedAfter` 游标正确回溯。

## 2. 筛选与质量控制
- **目标**: 优化 `scoreNode` 和 `gateKeepNode` 逻辑，针对每个分类筛选，确保核心分类（AI、Tech 等）均有优质文章入选。
- **操作**:
  - [ ] 改进分类偏好算法。
  - [ ] 确保 `AI`、`Tech` 财经、投资理财 等每个分类都能选出 3-5 条高质量入选。

## 3. 日报内容结构优化：添加“更多参考”列表
- **目标**: 在深度解读的新闻下方，添加一个简单的列表，展示“质量尚可但不做深度解读”的新闻。
- **操作**:
  - [ ] 修改 `generateDailyNode` 逻辑。
  - [ ] 过滤掉低质和内容重复的新闻。
  - [ ] 将“中等分数”的新闻流存在末尾列表中展示。

## 4. 重点突破：投资理财分类
- **目标**: 强化投资理财分发的专业度。
- **内容**:
  - [ ] 重点关注过去 24 小时内金融市场的重大影响。
  - [ ] 在洞察中给出关键信息的逻辑分析和推荐的操作建议。
  - [ ] 专门优化 `insightNode` 针对该分类的 Prompt。

## 5. 分类与权重逻辑深度优化
- **目标**: 解决财经、科技等非 AI 分类在日报中“消失”或“标签错误”的问题。
- **操作**:
  - [ ] **次要新闻 AI 自动化分类**：目前“更多 24h 资讯”直接使用抓取时的原始来源标签（如 `folo-list` 统一标记为 `ai`），导致非 AI 内容也被堆在 AI 领域下。需在 `score` 节点或 `generate-daily` 节点引入快速 AI 分类，实现备选新闻的精准归类。
  - [ ] **优化抓取平衡策略**：在 `fetch.ts` 中改进“熔断”逻辑，即便 `folo-list` 抓够了数量，也应确保从 `business`、`politics` 等特定源抓取一定比例的原始信息，防止源头缺失。
  - [ ] **调整评分偏好偏差**：在 `prompt.json` 中适当引入泛科技、泛财经的关注维度，降低评分系统对非 AI 内容的“由于不相关而导致的高分阻隔”。
  - [ ] **全局分类预处理**：考虑将分类逻辑提前到评分之前，或在评分时集成部分分类信息，使得 `score` 节点的“按分类名分配指标”功能能真正生效（目前因原始标签全是 `ai` 而失效）。

---
*上次更新于 2026-03-25*
