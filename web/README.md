# Legacy Web Prototype

`web/` 是早期的 Vite + React 原型，不是当前默认前端。

当前事实：

- 根目录 `npm run dev/build/start` 指向的是 [frontend/](../frontend)
- GitHub Actions 与当前文档默认描述的展示层也是 `frontend/`
- `web/` 仍保留在仓库里，主要用于历史参考或后续实验

如果你要修改当前线上展示，请优先查看 [frontend/README.md](../frontend/README.md)。
